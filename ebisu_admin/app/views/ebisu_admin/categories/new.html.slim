.row
  h1 New Category

  = form_with scope: :category, model: @category, url: categories_path, local: true do |form|
    = render "#{form.object.type.constantize.model_name.human.downcase}_form", category: @category, form: form
